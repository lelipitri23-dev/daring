<!DOCTYPE html>
<html lang="id">
<%- include('layout_head') %>
<body class="flex flex-col min-h-screen">
    
    <%- include('navbar') %>

    <main class="flex-grow container mx-auto px-4 py-8">
        <h2 class="text-2xl font-bold mb-6 border-l-4 border-blue-500 pl-4 text-white uppercase">
            <%= pageTitle %>
        </h2>
        
        <% if(mangas.length > 0) { %>
            <div class="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-4 gap-4">
                <% mangas.forEach(manga => { %>
                    <div class="bg-gray-800 rounded-lg overflow-hidden shadow-lg hover:-translate-y-1 transition duration-300 group">
                        <a href="/manga/<%= manga.slug %>">
                            <div class="h-64 bg-gray-700 relative overflow-hidden">
                                <% if (manga.thumb) { %>
                                    <img src="<%= manga.thumb %>" alt="<%= manga.title %>" class="w-full h-full object-cover group-hover:scale-105 transition duration-500" loading="lazy">
                                <% } else { %>
                                    <div class="w-full h-full flex items-center justify-center text-4xl">📚</div>
                                <% } %>
                                <div class="absolute bottom-0 w-full bg-gradient-to-t from-black to-transparent p-2">
                                    <span class="text-xs text-blue-300 block">
                                        <%= manga.metadata.type ? (typeof manga.metadata.type === 'object' ? manga.metadata.type.type : manga.metadata.type) : 'Komik' %>
                                    </span>
                                </div>
                            </div>
                            <div class="p-3">
                                <h3 class="text-sm font-semibold text-white line-clamp-2 mb-2"><%= manga.title %></h3>
                                <div class="flex justify-between items-center text-xs text-gray-400">
                                    <span>⭐ <%= manga.metadata.rating %></span>
                                    <span><%= manga.metadata.status %></span>
                                </div>
                            </div>
                        </a>
                    </div>
                <% }) %>
            </div>
        <% } else { %>
            <div class="flex flex-col items-center justify-center py-20">
                <div class="text-6xl mb-4">😔</div>
                <p class="text-xl text-gray-400 font-semibold">Tidak ditemukan</p>
                <p class="text-gray-500 mt-2 text-sm">Coba kata kunci lain atau kembali ke beranda.</p>
                <a href="/" class="mt-6 px-6 py-2 bg-blue-600 hover:bg-blue-500 text-white rounded-full transition">Kembali ke Home</a>
            </div>
        <% } %>
    </main>

    <%- include('footer') %>
</body>
</html>